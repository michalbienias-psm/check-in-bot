{
  "name": "slack-checkin-bot",
  "version": "1.0.0",
  "description": "Slack bot that sends weekly check-in DMs to all workspace users",
  "main": "app.js",
  "scripts": {
    "start": "node app.js"
  },
  "dependencies": {
    "@google-cloud/secret-manager": "^5.6.0",
    "@slack/bolt": "^3.13.0",
    "express": "^4.18.2",
    "node-cron": "^3.0.2"
  },
  "engines": {
    "node": ">=16"
  }
}
